# 新版预览与交接

项目在 D:\蓝旗鱼官网，本地预览端口 4186，仅绑定 127.0.0.1。私有 data 目录保存当前预览地址、测试访问资料、验证结果和截图，不进入 Git。

临时隧道重启后地址可能变化；电脑或服务停止会中断访问。

## 开发和验证

Node >=22.13，运行 npm ci。参考 .env.example 设置独立数据库与随机认证信息。预览需 PREVIEW_MODE=true 后 npm run build，再运行 npm run start -- -H 127.0.0.1 -p 4186。

验证脚本 scripts/verify-world.mjs 仅可用于独立测试数据库，会创建合成账号、邀请及上传文件，不要针对生产运行。脚本从私有测试配置或 TEST_PASSWORD 读取访问资料。

## 生产交接

遵循 AGENTS.md 和 README 的阿里云部署章节，先备份持久卷。将 data/resources 的六份下载文件复制到生产持久卷同名目录，不要放入 public。设置 SITE_URL=https://lqy-ai.com、PREVIEW_MODE=false、SECURE_COOKIES=true 后重新构建。生产会隐藏演示成员和模拟案例。

发布前核对创始人资料、课程授权范围与真实案例。Git 仅包含代码与公开媒体，不包含数据库、个人资料、课件原件或访问凭据。不要以预览数据库覆盖生产数据。
